# @package _global_

defaults:
  - _self_
  - datamodule: h5forecast.yaml
  - model: climax.yaml
  - logger: csv.yaml
  - trainer: gpu.yaml
  - paths: forecast_hpc.yaml
  - extras: default.yaml
  - hydra: default.yaml

task_name: "eval"

tags: ["eval"]

ckpt_path: null